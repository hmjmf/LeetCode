#include <vector>
#include <assert.h>
#include "helper.h"

using namespace std;

class Solution {
public:
  bool isMatch(string s, string p) {
        
    }

};

int main()
{
    Solution s;
    std::cout << s.isMatch("aa", "a") << std::endl;
    std::cout << s.isMatch("aa", "a*") << std::endl;
    std::cout << s.isMatch("aa", ".*") << std::endl;
    
}
